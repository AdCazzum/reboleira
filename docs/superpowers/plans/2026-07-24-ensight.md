# ENSight Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Un'estensione Chrome che rigenera l'interfaccia dei siti su misura per la persona, in base a un profilo cifrato puntato da un text record ENS, con inferenza generative-UI su 0G Compute e verifica umana con World ID.

**Architecture:** Un content script (mondo ISOLATED) estrae dalla pagina un `ContentGraph` semantico; un motore di generative UI produce una `UISpec` strutturata (schema vincolato, `refId` che puntano a nodi DOM reali); un renderer la disegna in un overlay shadow-DOM ricollegando le azioni ai nodi originali. Wallet, firma e scritture on-chain passano da uno script iniettato nel mondo MAIN della pagina. La logica pura (extractor, schema, renderer, tema, crypto, prompt) è testata in TDD; gli adapter di I/O (0G, ENS, World) sono thin e testati con mock, più verifica manuale su testnet.

**Tech Stack:** TypeScript · Vite + `@crxjs/vite-plugin` (MV3) · Preact (popup, onboarding, renderer) · Vitest + jsdom · ethers v6 · zod · `@0gfoundation/0g-compute-ts-sdk` (inferenza) · `@0glabs/0g-ts-sdk` (storage) · `@worldcoin/idkit`.

## Global Constraints

- **Budget zero**: solo testnet + locale. Sepolia per ENS, Galileo (chainId **16601**) per 0G. Nessuna API a pagamento; fallback su fixture pre-calcolate e/o Ollama locale.
- **Dati sensibili mai in chiaro fuori dal client/TEE**: il `PersonaProfile` è cifrato prima di lasciare il browser (AES-GCM, chiave da firma wallet). On-chain va solo il puntatore.
- **Chiave record ENS**: `app.ensight.profile` (puntatore blob 0G) e `app.ensight.human` (attestation World). Valori esatti, usare verbatim.
- **`refId` reali**: link e azioni nella `UISpec` provengono sempre da `data-ensight-id` presenti nel `ContentGraph`. Mai URL/azioni inventate: la validazione DEVE rifiutare `refId` sconosciuti.
- **Demo su pagine mock controllate** (`demo/page-a-news.html`, `demo/page-b-product.html`) e due personas (`demo/personas/persona-a.json`, `persona-b.json`).
- **Segreti da dashboard** stanno in `.env` (mai committati): `VITE_WORLD_APP_ID`, `VITE_WORLD_ACTION`, `VITE_SEPOLIA_RPC`, `VITE_ZEROG_RPC`, `VITE_ENS_NAME`. Committare solo `.env.example`.
- **Node ≥ 20** (WebCrypto `crypto.subtle` nativo, richiesto dai test crypto).

---

## File Structure

```
package.json · vite.config.ts · vitest.config.ts · tsconfig.json · .env.example
src/
  manifest.config.ts        # manifest MV3 via @crxjs
  config.ts                 # chain id, RPC, record keys, endpoint (da env)
  core/                     # LOGICA PURA — unit-tested TDD
    types.ts                # Block, ContentGraph, PersonaProfile, UITheme, UISpec
    content-extractor.ts    # DOM -> ContentGraph (+ tag data-ensight-id)
    uispec-schema.ts        # zod schema della UISpec
    uispec-validate.ts      # validazione struttura + integrità refId vs ContentGraph
    theme.ts                # PersonaProfile -> UITheme -> CSS vars
    crypto.ts               # deriveKey(firma) + AES-GCM encrypt/decrypt JSON
    prompt.ts               # ContentGraph + Profile -> payload inferenza
    renderer.ts             # UISpec + ContentGraph -> nodi shadow-DOM, rewire azioni
  services/                 # ADAPTER I/O — thin, mock-tested
    zerog-compute.ts        # requestUISpec() via SDK, parse+valida, throw su errore
    zerog-storage.ts        # uploadEncrypted()/download()
    ens.ts                  # readProfilePointer()/writeProfileRecords()
    world-id.ts             # attestationFromProof()
  content/
    bridge.ts               # protocollo postMessage content<->injected (tipi + callInjected)
    injected.ts             # MAIN world: window.ethereum (accounts, sign, setText)
    content-script.ts       # orchestratore: extract -> adapt -> render -> toggle
  background/service-worker.ts  # router messaggi + cache profilo di sessione
  ui/
    popup/popup.tsx         # stato, on/off, apri onboarding
    onboarding/onboarding.tsx # wizard: connect -> World -> form -> encrypt -> upload -> setText
demo/
  page-a-news.html · page-b-product.html
  personas/persona-a.json · persona-b.json
  fixtures/uispec-<page>-<persona>.json   # UISpec pre-calcolate (fallback demo)
tests/  (specchio di src/core e src/services)
```

Ordine dei task pensato perché **al Task 8 la demo funziona end-to-end senza rete** (con fixture), e i task 9+ aggiungono il percorso live on-chain/0G sopra una base già demo-abile.

---

### Task 1: Scaffold progetto (Vite + CRXJS + TS + Vitest)

**Files:**
- Create: `package.json`, `vite.config.ts`, `vitest.config.ts`, `tsconfig.json`, `.env.example`, `.gitignore` (append), `src/manifest.config.ts`, `src/content/content-script.ts` (stub), `src/config.ts`

**Interfaces:**
- Produces: comandi `npm run dev`, `npm run build`, `npm test`; estensione caricabile unpacked.

- [ ] **Step 1: Inizializza package e dipendenze**

```bash
npm init -y
npm i -D vite @crxjs/vite-plugin typescript vitest jsdom @types/chrome
npm i preact ethers@^6 zod
# SDK sponsor (nomi da confermare in docs; isolati dietro adapter):
npm i @0gfoundation/0g-compute-ts-sdk @0glabs/0g-ts-sdk @worldcoin/idkit
```

- [ ] **Step 2: Configura TypeScript**

`tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022", "module": "ESNext", "moduleResolution": "bundler",
    "jsx": "react-jsx", "jsxImportSource": "preact",
    "strict": true, "skipLibCheck": true, "types": ["chrome", "vite/client"],
    "lib": ["ES2022", "DOM", "DOM.Iterable"]
  },
  "include": ["src", "tests"]
}
```

- [ ] **Step 3: Manifest MV3**

`src/manifest.config.ts`:
```ts
import { defineManifest } from '@crxjs/vite-plugin';
export default defineManifest({
  manifest_version: 3,
  name: 'ENSight',
  version: '0.1.0',
  action: { default_popup: 'src/ui/popup/index.html' },
  background: { service_worker: 'src/background/service-worker.ts', type: 'module' },
  permissions: ['activeTab', 'scripting', 'storage'],
  host_permissions: ['<all_urls>'],
  content_scripts: [{
    matches: ['<all_urls>'],
    js: ['src/content/content-script.ts'],
    run_at: 'document_idle'
  }],
  web_accessible_resources: [{ resources: ['src/content/injected.ts'], matches: ['<all_urls>'] }]
});
```

- [ ] **Step 4: Vite + Vitest config**

`vite.config.ts`:
```ts
import { defineConfig } from 'vite';
import { crx } from '@crxjs/vite-plugin';
import preact from '@preact/preset-vite';
import manifest from './src/manifest.config';
export default defineConfig({ plugins: [preact(), crx({ manifest })] });
```
(Install `@preact/preset-vite` as dev dep.)

`vitest.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
export default defineConfig({
  test: { environment: 'node', include: ['tests/**/*.test.ts'] }
});
```
(DOM tests opt-in via `// @vitest-environment jsdom` header per file.)

- [ ] **Step 5: Config + stub content script + .env.example**

`src/config.ts`:
```ts
export const CONFIG = {
  sepoliaRpc: import.meta.env.VITE_SEPOLIA_RPC as string,
  zerogRpc: import.meta.env.VITE_ZEROG_RPC as string,
  zerogChainId: 16601,
  ensName: import.meta.env.VITE_ENS_NAME as string,
  worldAppId: import.meta.env.VITE_WORLD_APP_ID as string,
  worldAction: import.meta.env.VITE_WORLD_ACTION as string,
  recordKeys: { profile: 'app.ensight.profile', human: 'app.ensight.human' }
} as const;
```
`src/content/content-script.ts`: `console.log('[ENSight] content script attivo su', location.href);`

`.env.example`:
```
VITE_SEPOLIA_RPC=https://sepolia.infura.io/v3/<key>
VITE_ZEROG_RPC=https://evmrpc-testnet.0g.ai
VITE_ENS_NAME=yourname.eth
VITE_WORLD_APP_ID=app_staging_xxx
VITE_WORLD_ACTION=verify-human
```
Append to `.gitignore`: `.env`, `node_modules`, `dist`.

- [ ] **Step 6: Verifica build e caricamento**

Run: `npm run build`
Expected: build ok, cartella `dist/`. Caricare `dist/` da `chrome://extensions` (Developer mode → Load unpacked), aprire una pagina qualunque → in console appare `[ENSight] content script attivo`.

- [ ] **Step 7: Commit**

```bash
git add -A && git commit -m "chore: scaffold estensione ENSight (Vite+CRXJS+TS+Vitest)"
```

---

### Task 2: Tipi core

**Files:**
- Create: `src/core/types.ts`
- Test: (nessuno — solo tipi)

**Interfaces:**
- Produces: `Block`, `BlockType`, `ContentGraph`, `PersonaProfile`, `UITheme`, `UIContentItem`, `UISection`, `UISpec`. Tutti i task successivi consumano questi tipi.

- [ ] **Step 1: Scrivi i tipi**

`src/core/types.ts`:
```ts
export type BlockType = 'heading'|'paragraph'|'list'|'image'|'link'|'action'|'form'|'nav';

export interface Block {
  id: string;                 // "block-12", stabile nella singola estrazione
  type: BlockType;
  text?: string;              // testo visibile (heading/paragraph/link/action label)
  items?: string[];           // per list
  href?: string;              // per link
  src?: string;               // per image
  level?: number;             // per heading 1-6
}
export interface ContentGraph { url: string; title: string; blocks: Block[]; }

export interface PersonaProfile {
  version: 1;
  language: string;                                   // BCP-47, es "it"
  readingLevel: 'simple'|'standard'|'expert';
  accessibility: { dyslexiaFriendly: boolean; highContrast: boolean; largeText: boolean; reduceClutter: boolean; };
  expertiseDomains: string[];
  tone: 'plain'|'neutral'|'technical';
}

export interface UITheme {
  fontScale: number; contrast: 'normal'|'high'; font: 'system'|'dyslexic'|'serif';
  lineSpacing: number; colorMode: 'light'|'dark'|'auto'; density: 'comfortable'|'compact';
}
export type UIContentItem =
  | { type: 'text'; text: string }
  | { type: 'list'; items: string[] }
  | { type: 'image'; refId: string }
  | { type: 'action'; refId: string; label: string };
export interface UISection {
  role: 'primary'|'summary'|'content'|'actions'|'navigation'|'aside'|'hidden';
  priority: number; heading?: string; content: UIContentItem[]; sourceRefs: string[];
}
export interface UISpec {
  theme: UITheme; language: string; readingLevel: 'simple'|'standard'|'expert';
  sections: UISection[]; hidden: string[];
}
```

- [ ] **Step 2: Verifica compilazione**

Run: `npx tsc --noEmit`
Expected: nessun errore.

- [ ] **Step 3: Commit**

```bash
git add src/core/types.ts && git commit -m "feat(core): tipi condivisi ContentGraph/PersonaProfile/UISpec"
```

---

### Task 3: Content Extractor (DOM → ContentGraph)

**Files:**
- Create: `src/core/content-extractor.ts`
- Test: `tests/core/content-extractor.test.ts`

**Interfaces:**
- Consumes: `Block`, `ContentGraph` da `core/types`.
- Produces: `extractContentGraph(doc: Document): ContentGraph`. Effetto collaterale voluto: assegna `data-ensight-id="block-N"` sugli elementi estratti (usato dal renderer per il rewire).

- [ ] **Step 1: Scrivi i test (jsdom)**

`tests/core/content-extractor.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { extractContentGraph } from '../../src/core/content-extractor';

function docFrom(html: string): Document {
  const d = document.implementation.createHTMLDocument('t');
  d.body.innerHTML = html; return d;
}

describe('extractContentGraph', () => {
  it('estrae heading, paragrafo e link con id progressivi', () => {
    const doc = docFrom('<h1>Titolo</h1><p>Ciao mondo</p><a href="/x">Vai</a>');
    const g = extractContentGraph(doc);
    expect(g.blocks.map(b => b.type)).toEqual(['heading','paragraph','link']);
    expect(g.blocks[0]).toMatchObject({ type: 'heading', level: 1, text: 'Titolo' });
    expect(g.blocks[2]).toMatchObject({ type: 'link', href: '/x', text: 'Vai' });
  });

  it('tagga il DOM con data-ensight-id coerenti con gli id dei block', () => {
    const doc = docFrom('<p>uno</p><p>due</p>');
    const g = extractContentGraph(doc);
    const tagged = doc.querySelectorAll('[data-ensight-id]');
    expect(tagged.length).toBe(2);
    expect((tagged[0] as HTMLElement).dataset.ensightId).toBe(g.blocks[0].id);
  });

  it('classifica button come action e ul come list', () => {
    const doc = docFrom('<button>Compra</button><ul><li>a</li><li>b</li></ul>');
    const g = extractContentGraph(doc);
    expect(g.blocks[0]).toMatchObject({ type: 'action', text: 'Compra' });
    expect(g.blocks[1]).toMatchObject({ type: 'list', items: ['a','b'] });
  });

  it('ignora script/style e nodi vuoti', () => {
    const doc = docFrom('<style>.x{}</style><p>   </p><p>ok</p><script>1</script>');
    const g = extractContentGraph(doc);
    expect(g.blocks.map(b => b.text)).toEqual(['ok']);
  });
});
```

- [ ] **Step 2: Esegui i test (devono fallire)**

Run: `npx vitest run tests/core/content-extractor.test.ts`
Expected: FAIL (`extractContentGraph` non definita).

- [ ] **Step 3: Implementa**

`src/core/content-extractor.ts`:
```ts
import type { Block, BlockType, ContentGraph } from './types';

const SKIP = new Set(['SCRIPT','STYLE','NOSCRIPT','TEMPLATE','SVG']);

function classify(el: Element): { type: BlockType; extra: Partial<Block> } | null {
  const tag = el.tagName;
  if (/^H[1-6]$/.test(tag)) return { type: 'heading', extra: { level: +tag[1] } };
  if (tag === 'P') return { type: 'paragraph', extra: {} };
  if (tag === 'UL' || tag === 'OL')
    return { type: 'list', extra: { items: [...el.querySelectorAll('li')].map(li => li.textContent!.trim()).filter(Boolean) } };
  if (tag === 'BUTTON' || (tag === 'INPUT' && (el as HTMLInputElement).type === 'submit'))
    return { type: 'action', extra: {} };
  if (tag === 'A') return { type: 'link', extra: { href: (el as HTMLAnchorElement).getAttribute('href') ?? '' } };
  if (tag === 'IMG') return { type: 'image', extra: { src: (el as HTMLImageElement).getAttribute('src') ?? '' } };
  if (tag === 'NAV') return { type: 'nav', extra: {} };
  if (tag === 'FORM') return { type: 'form', extra: {} };
  return null;
}

export function extractContentGraph(doc: Document): ContentGraph {
  const blocks: Block[] = [];
  let n = 0;
  const walk = (el: Element) => {
    if (SKIP.has(el.tagName)) return;
    const c = classify(el);
    if (c) {
      const text = c.type === 'list' ? undefined : (el.textContent ?? '').trim();
      if (c.type === 'list' || c.type === 'image' || (text && text.length)) {
        const id = `block-${n++}`;
        (el as HTMLElement).dataset.ensightId = id;
        blocks.push({ id, type: c.type, ...(text ? { text } : {}), ...c.extra });
        return; // non ridiscendere dentro un blocco già catturato
      }
    }
    for (const child of el.children) walk(child);
  };
  walk(doc.body);
  return { url: doc.location?.href ?? '', title: doc.title, blocks };
}
```

- [ ] **Step 4: Esegui i test (devono passare)**

Run: `npx vitest run tests/core/content-extractor.test.ts`
Expected: PASS (4 test).

- [ ] **Step 5: Commit**

```bash
git add src/core/content-extractor.ts tests/core/content-extractor.test.ts
git commit -m "feat(core): content extractor DOM->ContentGraph con tagging data-ensight-id"
```

---

### Task 4: Schema UISpec (zod) + validazione refId

**Files:**
- Create: `src/core/uispec-schema.ts`, `src/core/uispec-validate.ts`
- Test: `tests/core/uispec-validate.test.ts`

**Interfaces:**
- Consumes: `ContentGraph`, `UISpec` da `core/types`.
- Produces: `parseUISpec(raw: unknown): UISpec` (throw su schema invalido) e `validateRefs(spec: UISpec, graph: ContentGraph): void` (throw su `refId`/`sourceRef` sconosciuti). `assertValidUISpec(raw, graph): UISpec` che compone i due.

- [ ] **Step 1: Scrivi i test**

`tests/core/uispec-validate.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { assertValidUISpec } from '../../src/core/uispec-validate';
import type { ContentGraph } from '../../src/core/types';

const graph: ContentGraph = { url: '', title: 't', blocks: [
  { id: 'block-0', type: 'heading', text: 'H', level: 1 },
  { id: 'block-1', type: 'action', text: 'Buy' },
]};
const good = {
  theme: { fontScale: 1.2, contrast: 'high', font: 'dyslexic', lineSpacing: 1.6, colorMode: 'light', density: 'comfortable' },
  language: 'it', readingLevel: 'simple',
  sections: [{ role: 'primary', priority: 1, heading: 'H',
    content: [{ type: 'text', text: 'ciao' }, { type: 'action', refId: 'block-1', label: 'Compra' }],
    sourceRefs: ['block-0'] }],
  hidden: []
};

describe('assertValidUISpec', () => {
  it('accetta una spec valida con refId esistenti', () => {
    expect(() => assertValidUISpec(good, graph)).not.toThrow();
  });
  it('rifiuta schema malformato (readingLevel sconosciuto)', () => {
    expect(() => assertValidUISpec({ ...good, readingLevel: 'genius' }, graph)).toThrow();
  });
  it('rifiuta refId inesistente in un action', () => {
    const bad = structuredClone(good);
    (bad.sections[0].content[1] as any).refId = 'block-999';
    expect(() => assertValidUISpec(bad, graph)).toThrow(/block-999/);
  });
  it('rifiuta sourceRef inesistente', () => {
    const bad = structuredClone(good);
    bad.sections[0].sourceRefs = ['block-42'];
    expect(() => assertValidUISpec(bad, graph)).toThrow(/block-42/);
  });
});
```

- [ ] **Step 2: Esegui i test (devono fallire)**

Run: `npx vitest run tests/core/uispec-validate.test.ts`
Expected: FAIL (moduli non esistenti).

- [ ] **Step 3: Implementa lo schema**

`src/core/uispec-schema.ts`:
```ts
import { z } from 'zod';
export const themeSchema = z.object({
  fontScale: z.number().min(0.5).max(3),
  contrast: z.enum(['normal','high']),
  font: z.enum(['system','dyslexic','serif']),
  lineSpacing: z.number().min(1).max(3),
  colorMode: z.enum(['light','dark','auto']),
  density: z.enum(['comfortable','compact'])
});
const item = z.discriminatedUnion('type', [
  z.object({ type: z.literal('text'), text: z.string() }),
  z.object({ type: z.literal('list'), items: z.array(z.string()) }),
  z.object({ type: z.literal('image'), refId: z.string() }),
  z.object({ type: z.literal('action'), refId: z.string(), label: z.string() })
]);
export const uiSpecSchema = z.object({
  theme: themeSchema,
  language: z.string(),
  readingLevel: z.enum(['simple','standard','expert']),
  sections: z.array(z.object({
    role: z.enum(['primary','summary','content','actions','navigation','aside','hidden']),
    priority: z.number(), heading: z.string().optional(),
    content: z.array(item), sourceRefs: z.array(z.string())
  })),
  hidden: z.array(z.string())
});
```

- [ ] **Step 4: Implementa parse + validazione refId**

`src/core/uispec-validate.ts`:
```ts
import { uiSpecSchema } from './uispec-schema';
import type { ContentGraph, UISpec } from './types';

export function parseUISpec(raw: unknown): UISpec {
  return uiSpecSchema.parse(raw) as UISpec;
}
export function validateRefs(spec: UISpec, graph: ContentGraph): void {
  const known = new Set(graph.blocks.map(b => b.id));
  const check = (id: string) => { if (!known.has(id)) throw new Error(`refId sconosciuto: ${id}`); };
  for (const s of spec.sections) {
    s.sourceRefs.forEach(check);
    for (const c of s.content) if ('refId' in c) check(c.refId);
  }
  spec.hidden.forEach(check);
}
export function assertValidUISpec(raw: unknown, graph: ContentGraph): UISpec {
  const spec = parseUISpec(raw);
  validateRefs(spec, graph);
  return spec;
}
```

- [ ] **Step 5: Esegui i test (devono passare)**

Run: `npx vitest run tests/core/uispec-validate.test.ts`
Expected: PASS (4 test).

- [ ] **Step 6: Commit**

```bash
git add src/core/uispec-schema.ts src/core/uispec-validate.ts tests/core/uispec-validate.test.ts
git commit -m "feat(core): schema zod UISpec + validazione integrità refId"
```

---

### Task 5: Tema (PersonaProfile → UITheme → CSS vars)

**Files:**
- Create: `src/core/theme.ts`
- Test: `tests/core/theme.test.ts`

**Interfaces:**
- Consumes: `PersonaProfile`, `UITheme`.
- Produces: `profileToTheme(p: PersonaProfile): UITheme` e `themeToCssVars(t: UITheme): Record<string,string>`.

- [ ] **Step 1: Scrivi i test**

`tests/core/theme.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { profileToTheme, themeToCssVars } from '../../src/core/theme';
import type { PersonaProfile } from '../../src/core/types';

const base: PersonaProfile = {
  version: 1, language: 'it', readingLevel: 'simple',
  accessibility: { dyslexiaFriendly: true, highContrast: false, largeText: true, reduceClutter: true },
  expertiseDomains: [], tone: 'plain'
};

describe('profileToTheme', () => {
  it('dislessia -> font dyslexic e lineSpacing ampio', () => {
    const t = profileToTheme(base);
    expect(t.font).toBe('dyslexic');
    expect(t.lineSpacing).toBeGreaterThanOrEqual(1.6);
  });
  it('largeText -> fontScale > 1', () => {
    expect(profileToTheme(base).fontScale).toBeGreaterThan(1);
  });
  it('highContrast -> contrast high', () => {
    const t = profileToTheme({ ...base, accessibility: { ...base.accessibility, highContrast: true } });
    expect(t.contrast).toBe('high');
  });
});
describe('themeToCssVars', () => {
  it('mappa fontScale e lineSpacing su custom properties', () => {
    const vars = themeToCssVars(profileToTheme(base));
    expect(vars['--ens-font-scale']).toBeDefined();
    expect(vars['--ens-line-spacing']).toBeDefined();
  });
});
```

- [ ] **Step 2: Esegui i test (devono fallire)**

Run: `npx vitest run tests/core/theme.test.ts` → FAIL.

- [ ] **Step 3: Implementa**

`src/core/theme.ts`:
```ts
import type { PersonaProfile, UITheme } from './types';
export function profileToTheme(p: PersonaProfile): UITheme {
  const a = p.accessibility;
  return {
    fontScale: a.largeText ? 1.35 : 1,
    contrast: a.highContrast ? 'high' : 'normal',
    font: a.dyslexiaFriendly ? 'dyslexic' : 'system',
    lineSpacing: a.dyslexiaFriendly ? 1.8 : 1.4,
    colorMode: 'auto',
    density: a.reduceClutter ? 'comfortable' : 'compact'
  };
}
export function themeToCssVars(t: UITheme): Record<string,string> {
  const fontFamily = t.font === 'dyslexic'
    ? 'OpenDyslexic, "Comic Sans MS", sans-serif'
    : t.font === 'serif' ? 'Georgia, serif' : 'system-ui, sans-serif';
  return {
    '--ens-font-scale': String(t.fontScale),
    '--ens-line-spacing': String(t.lineSpacing),
    '--ens-font-family': fontFamily,
    '--ens-fg': t.contrast === 'high' ? '#000' : '#1a1a1a',
    '--ens-bg': t.contrast === 'high' ? '#fff' : '#fafafa',
    '--ens-gap': t.density === 'comfortable' ? '1.5rem' : '0.75rem'
  };
}
```

- [ ] **Step 4: Esegui i test (devono passare)**

Run: `npx vitest run tests/core/theme.test.ts` → PASS.

- [ ] **Step 5: Commit**

```bash
git add src/core/theme.ts tests/core/theme.test.ts
git commit -m "feat(core): mapping profilo->tema->CSS vars"
```

---

### Task 6: Renderer (UISpec + ContentGraph → shadow-DOM, rewire azioni)

**Files:**
- Create: `src/core/renderer.ts`
- Test: `tests/core/renderer.test.ts`

**Interfaces:**
- Consumes: `UISpec`, `ContentGraph`, `themeToCssVars`.
- Produces: `renderUISpec(spec: UISpec, doc: Document): HTMLElement` — costruisce e ritorna un `<div>` root (con shadow root interno) contenente le sezioni ordinate per `priority`, esclusi i `role:'hidden'`. Per un item `action`/`image` risolve `[data-ensight-id="<refId>"]` nel `doc`; per l'azione un click sul nodo generato invoca `.click()` sull'elemento originale.

- [ ] **Step 1: Scrivi i test (jsdom)**

`tests/core/renderer.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest';
import { renderUISpec } from '../../src/core/renderer';
import type { UISpec } from '../../src/core/types';

const theme = { fontScale:1, contrast:'normal', font:'system', lineSpacing:1.4, colorMode:'auto', density:'comfortable' } as const;

function specWithAction(): UISpec {
  return { theme, language:'it', readingLevel:'standard', hidden:[], sections:[
    { role:'primary', priority:2, heading:'Sotto', content:[{type:'text',text:'B'}], sourceRefs:[] },
    { role:'actions', priority:1, content:[{type:'action',refId:'block-1',label:'Compra ora'}], sourceRefs:[] },
  ]};
}

describe('renderUISpec', () => {
  it('ordina le sezioni per priority crescente', () => {
    document.body.innerHTML = '<button data-ensight-id="block-1">orig</button>';
    const root = renderUISpec(specWithAction(), document);
    const headings = root.shadowRoot!.querySelectorAll('[data-role]');
    expect((headings[0] as HTMLElement).dataset.role).toBe('actions');
  });
  it('un click sull\'azione generata attiva il click sull\'originale', () => {
    document.body.innerHTML = '<button data-ensight-id="block-1">orig</button>';
    const spy = vi.fn();
    document.querySelector('button')!.addEventListener('click', spy);
    const root = renderUISpec(specWithAction(), document);
    (root.shadowRoot!.querySelector('button[data-ref="block-1"]') as HTMLElement).click();
    expect(spy).toHaveBeenCalledOnce();
  });
  it('usa il testo riscritto dalla spec, non quello originale', () => {
    document.body.innerHTML = '<button data-ensight-id="block-1">orig</button>';
    const root = renderUISpec(specWithAction(), document);
    expect(root.shadowRoot!.textContent).toContain('Compra ora');
    expect(root.shadowRoot!.textContent).not.toContain('orig');
  });
});
```

- [ ] **Step 2: Esegui i test (devono fallire)**

Run: `npx vitest run tests/core/renderer.test.ts` → FAIL.

- [ ] **Step 3: Implementa (DOM puro, niente framework nel renderer core)**

`src/core/renderer.ts`:
```ts
import type { UISpec, UISection, UIContentItem } from './types';
import { themeToCssVars } from './theme';

function renderItem(doc: Document, item: UIContentItem): Node {
  if (item.type === 'text') { const p = doc.createElement('p'); p.textContent = item.text; return p; }
  if (item.type === 'list') {
    const ul = doc.createElement('ul');
    item.items.forEach(t => { const li = doc.createElement('li'); li.textContent = t; ul.appendChild(li); });
    return ul;
  }
  if (item.type === 'image') {
    const orig = doc.querySelector<HTMLImageElement>(`[data-ensight-id="${item.refId}"]`);
    const img = doc.createElement('img');
    if (orig) { img.src = orig.src; img.alt = orig.alt; }
    return img;
  }
  // action
  const btn = doc.createElement('button');
  btn.textContent = item.label;
  btn.dataset.ref = item.refId;
  btn.addEventListener('click', () => {
    doc.querySelector<HTMLElement>(`[data-ensight-id="${item.refId}"]`)?.click();
  });
  return btn;
}

function renderSection(doc: Document, s: UISection): HTMLElement {
  const sec = doc.createElement('section');
  sec.dataset.role = s.role;
  if (s.heading) { const h = doc.createElement('h2'); h.textContent = s.heading; sec.appendChild(h); }
  s.content.forEach(c => sec.appendChild(renderItem(doc, c)));
  return sec;
}

export function renderUISpec(spec: UISpec, doc: Document): HTMLElement {
  const root = doc.createElement('div');
  root.id = 'ensight-root';
  const shadow = root.attachShadow({ mode: 'open' });
  const style = doc.createElement('style');
  const vars = Object.entries(themeToCssVars(spec.theme)).map(([k,v]) => `${k}:${v}`).join(';');
  style.textContent = `:host{all:initial;${vars};display:block;font-family:var(--ens-font-family);
    color:var(--ens-fg);background:var(--ens-bg);font-size:calc(1rem*var(--ens-font-scale));
    line-height:var(--ens-line-spacing);padding:2rem;}
    section{margin-bottom:var(--ens-gap);} button{font:inherit;padding:.6em 1em;cursor:pointer;}`;
  shadow.appendChild(style);
  [...spec.sections].filter(s => s.role !== 'hidden')
    .sort((a,b) => a.priority - b.priority)
    .forEach(s => shadow.appendChild(renderSection(doc, s)));
  return root;
}
```

- [ ] **Step 4: Esegui i test (devono passare)**

Run: `npx vitest run tests/core/renderer.test.ts` → PASS (3 test).

- [ ] **Step 5: Commit**

```bash
git add src/core/renderer.ts tests/core/renderer.test.ts
git commit -m "feat(core): renderer UISpec->shadow-DOM con rewire azioni ai nodi reali"
```

---

### Task 7: Pagine mock + personas + fixture UISpec

**Files:**
- Create: `demo/page-a-news.html`, `demo/page-b-product.html`, `demo/personas/persona-a.json`, `demo/personas/persona-b.json`, `demo/fixtures/uispec-a-personaA.json`, `demo/fixtures/uispec-a-personaB.json`, `demo/fixtures/uispec-b-personaA.json`, `demo/fixtures/uispec-b-personaB.json`
- Test: `tests/demo/fixtures.test.ts`

**Interfaces:**
- Consumes: extractor + `assertValidUISpec` (test di coerenza).
- Produces: contenuti mock verificati e 4 fixture di fallback valide contro il proprio content graph.

- [ ] **Step 1: Crea le pagine mock**

`demo/page-a-news.html` — articolo denso con clutter (nav, banner, sidebar, testo lungo, un CTA `<button>`). `demo/page-b-product.html` — pagina prodotto/doc con specifiche tecniche, tabella, CTA `Add to cart`. Ogni elemento significativo è markup HTML reale (no data-ensight-id: lo mette l'extractor a runtime). Verificare a occhio aprendole nel browser.

- [ ] **Step 2: Crea i due profili persona**

`demo/personas/persona-a.json`:
```json
{ "version": 1, "language": "it", "readingLevel": "simple",
  "accessibility": { "dyslexiaFriendly": true, "highContrast": false, "largeText": true, "reduceClutter": true },
  "expertiseDomains": [], "tone": "plain" }
```
`demo/personas/persona-b.json`:
```json
{ "version": 1, "language": "en", "readingLevel": "expert",
  "accessibility": { "dyslexiaFriendly": false, "highContrast": true, "largeText": true, "reduceClutter": false },
  "expertiseDomains": ["finance","crypto"], "tone": "technical" }
```

- [ ] **Step 3: Scrivi il test di coerenza fixture**

`tests/demo/fixtures.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { readFileSync } from 'node:fs';
import { extractContentGraph } from '../../src/core/content-extractor';
import { assertValidUISpec } from '../../src/core/uispec-validate';

const cases = [
  ['a', 'page-a-news.html'], ['b', 'page-b-product.html']
] as const;
const personas = ['personaA','personaB'] as const;

describe('fixtures di fallback', () => {
  for (const [key, file] of cases) for (const persona of personas) {
    it(`uispec-${key}-${persona} è valida contro il content graph di ${file}`, () => {
      const html = readFileSync(`demo/${file}`, 'utf8');
      const doc = document.implementation.createHTMLDocument('t');
      doc.body.innerHTML = html.replace(/^[\s\S]*?<body[^>]*>|<\/body>[\s\S]*$/g, '');
      const graph = extractContentGraph(doc);
      const spec = JSON.parse(readFileSync(`demo/fixtures/uispec-${key}-${persona}.json`, 'utf8'));
      expect(() => assertValidUISpec(spec, graph)).not.toThrow();
    });
  }
});
```

- [ ] **Step 4: Genera i content graph e scrivi le fixture**

Esegui uno script una tantum per stampare il content graph di ogni pagina (`npx tsx scripts/dump-graph.ts demo/page-a-news.html`) così conosci gli `id`. Scrivi a mano le 4 fixture `demo/fixtures/uispec-*.json` con `refId`/`sourceRefs` reali presi da quei graph, tema coerente con la persona (persona A: font dyslexic, testo semplificato in italiano, summary-first, molte sezioni `hidden`; persona B: high contrast, testo tecnico in inglese, denso). Riesegui il test finché passa.

Run: `npx vitest run tests/demo/fixtures.test.ts`
Expected: PASS (4 test) — se fallisce con "refId sconosciuto", correggi gli id nella fixture.

- [ ] **Step 5: Commit**

```bash
git add demo tests/demo/fixtures.test.ts scripts/dump-graph.ts
git commit -m "feat(demo): pagine mock, personas e fixture UISpec di fallback validate"
```

---

### Task 8: Orchestrazione content script + toggle (percorso fixture — DEMO END-TO-END)

**Files:**
- Modify: `src/content/content-script.ts`
- Create: `src/content/adapt.ts` (strategia di adattamento con fallback), `demo/fixtures/index.ts` (map pagina+persona→fixture importata)
- Test: `tests/content/adapt.test.ts`

**Interfaces:**
- Consumes: `extractContentGraph`, `assertValidUISpec`, `renderUISpec`.
- Produces: `adaptPage(doc, profile, getSpec): Promise<HTMLElement>` dove `getSpec(graph, profile): Promise<unknown>` è iniettata (fixture o 0G); `adaptPage` valida e rende, e su errore di `getSpec` rilancia. Content script: monta root, nasconde `<body>` originale, gestisce toggle.

- [ ] **Step 1: Scrivi il test di `adaptPage`**

`tests/content/adapt.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect, vi } from 'vitest';
import { adaptPage } from '../../src/content/adapt';
import type { PersonaProfile } from '../../src/core/types';

const profile: PersonaProfile = { version:1, language:'it', readingLevel:'simple',
  accessibility:{dyslexiaFriendly:true,highContrast:false,largeText:true,reduceClutter:true},
  expertiseDomains:[], tone:'plain' };

const validSpec = { theme:{fontScale:1,contrast:'normal',font:'system',lineSpacing:1.4,colorMode:'auto',density:'comfortable'},
  language:'it', readingLevel:'simple', hidden:[], sections:[{role:'primary',priority:1,content:[{type:'text',text:'ok'}],sourceRefs:[]}] };

describe('adaptPage', () => {
  it('rende la spec fornita da getSpec', async () => {
    document.body.innerHTML = '<p>orig</p>';
    const root = await adaptPage(document, profile, async () => validSpec);
    expect(root.shadowRoot!.textContent).toContain('ok');
  });
  it('propaga l\'errore se getSpec fallisce (il chiamante deciderà il fallback)', async () => {
    document.body.innerHTML = '<p>orig</p>';
    await expect(adaptPage(document, profile, async () => { throw new Error('rete'); })).rejects.toThrow('rete');
  });
});
```

- [ ] **Step 2: Esegui (deve fallire)**

Run: `npx vitest run tests/content/adapt.test.ts` → FAIL.

- [ ] **Step 3: Implementa `adapt.ts`**

`src/content/adapt.ts`:
```ts
import type { PersonaProfile } from '../core/types';
import { extractContentGraph } from '../core/content-extractor';
import { assertValidUISpec } from '../core/uispec-validate';
import { renderUISpec } from '../core/renderer';

export type SpecProvider = (graph: ReturnType<typeof extractContentGraph>, profile: PersonaProfile) => Promise<unknown>;

export async function adaptPage(doc: Document, profile: PersonaProfile, getSpec: SpecProvider): Promise<HTMLElement> {
  const graph = extractContentGraph(doc);
  const raw = await getSpec(graph, profile);
  const spec = assertValidUISpec(raw, graph);
  return renderUISpec(spec, doc);
}
```

- [ ] **Step 4: Esegui (deve passare)**

Run: `npx vitest run tests/content/adapt.test.ts` → PASS.

- [ ] **Step 5: Cabla il content script con fixture + toggle**

`demo/fixtures/index.ts`: importa le 4 JSON e le mappa per `location.pathname` (contiene `page-a`/`page-b`) e persona attiva; esporta `fixtureFor(url, persona)`. Persona attiva in dev: letta da `chrome.storage.local` (default `personaA`).

`src/content/content-script.ts`:
```ts
import { adaptPage } from './adapt';
import { fixtureFor } from '../../demo/fixtures';
let adaptedRoot: HTMLElement | null = null;
const originals: HTMLElement[] = [];

async function activePersona(): Promise<'personaA'|'personaB'> {
  const { persona } = await chrome.storage.local.get('persona');
  return persona ?? 'personaA';
}
async function loadProfile(persona: string) { /* dev: import statico dei personas JSON */ }

async function enable() {
  const persona = await activePersona();
  const profile = await loadProfile(persona);
  adaptedRoot = await adaptPage(document, profile, async () => fixtureFor(location.href, persona));
  [...document.body.children].forEach(c => { (c as HTMLElement).style.display = 'none'; originals.push(c as HTMLElement); });
  document.body.appendChild(adaptedRoot);
}
function disable() {
  adaptedRoot?.remove(); adaptedRoot = null;
  originals.forEach(c => c.style.display = ''); originals.length = 0;
}
chrome.runtime.onMessage.addListener((msg) => {
  if (msg?.type === 'ensight:toggle') { adaptedRoot ? disable() : enable(); }
});
```
(Nota: `loadProfile` in dev importa i JSON da `demo/personas`; verrà sostituito dal percorso 0G nei task successivi.)

- [ ] **Step 6: Verifica manuale end-to-end (senza rete)**

Run: `npm run build`, ricarica l'estensione, apri `demo/page-a-news.html` (file:// o server statico), invoca il toggle (via popup temporaneo o `chrome.runtime.sendMessage` dalla console del service worker). Expected: la pagina si trasforma secondo `uispec-a-personaA`; cambiando persona in `chrome.storage.local` e ri-togglando appare la versione B. **Milestone: demo funzionante a costo zero, nessuna dipendenza esterna.**

- [ ] **Step 7: Commit**

```bash
git add src/content demo/fixtures/index.ts tests/content/adapt.test.ts
git commit -m "feat(content): orchestrazione adapt+toggle su fixture (demo e2e offline)"
```

---

### Task 9: Crypto (chiave da firma + AES-GCM)

**Files:**
- Create: `src/core/crypto.ts`
- Test: `tests/core/crypto.test.ts`

**Interfaces:**
- Produces: `deriveKey(signatureHex: string): Promise<CryptoKey>`, `encryptJson(key, obj): Promise<string>` (base64 `iv|ciphertext`), `decryptJson<T>(key, blob: string): Promise<T>`. `SIGN_MESSAGE` costante esportata (messaggio deterministico da firmare).

- [ ] **Step 1: Scrivi i test**

`tests/core/crypto.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { deriveKey, encryptJson, decryptJson } from '../../src/core/crypto';

const sig = '0x' + 'ab'.repeat(65); // firma fittizia deterministica

describe('crypto', () => {
  it('round-trip encrypt/decrypt ritorna l\'oggetto originale', async () => {
    const key = await deriveKey(sig);
    const blob = await encryptJson(key, { hello: 'mondo', n: 42 });
    expect(await decryptJson(key, blob)).toEqual({ hello: 'mondo', n: 42 });
  });
  it('chiave da firma diversa non decifra', async () => {
    const k1 = await deriveKey(sig);
    const k2 = await deriveKey('0x' + 'cd'.repeat(65));
    const blob = await encryptJson(k1, { x: 1 });
    await expect(decryptJson(k2, blob)).rejects.toBeDefined();
  });
});
```

- [ ] **Step 2: Esegui (deve fallire)**

Run: `npx vitest run tests/core/crypto.test.ts` → FAIL.

- [ ] **Step 3: Implementa**

`src/core/crypto.ts`:
```ts
export const SIGN_MESSAGE = 'ENSight profile encryption key v1';
const enc = new TextEncoder(); const dec = new TextDecoder();
const b64 = (b: ArrayBuffer) => btoa(String.fromCharCode(...new Uint8Array(b)));
const unb64 = (s: string) => Uint8Array.from(atob(s), c => c.charCodeAt(0));

export async function deriveKey(signatureHex: string): Promise<CryptoKey> {
  const raw = unb64(btoa(signatureHex)); // bytes deterministici dalla firma
  const base = await crypto.subtle.importKey('raw', raw, 'HKDF', false, ['deriveKey']);
  return crypto.subtle.deriveKey(
    { name: 'HKDF', hash: 'SHA-256', salt: enc.encode('ensight'), info: enc.encode('profile') },
    base, { name: 'AES-GCM', length: 256 }, false, ['encrypt','decrypt']);
}
export async function encryptJson(key: CryptoKey, obj: unknown): Promise<string> {
  const iv = crypto.getRandomValues(new Uint8Array(12));
  const ct = await crypto.subtle.encrypt({ name:'AES-GCM', iv }, key, enc.encode(JSON.stringify(obj)));
  return b64(iv.buffer) + '.' + b64(ct);
}
export async function decryptJson<T>(key: CryptoKey, blob: string): Promise<T> {
  const [ivB, ctB] = blob.split('.');
  const pt = await crypto.subtle.decrypt({ name:'AES-GCM', iv: unb64(ivB) }, key, unb64(ctB));
  return JSON.parse(dec.decode(pt)) as T;
}
```

- [ ] **Step 4: Esegui (deve passare)**

Run: `npx vitest run tests/core/crypto.test.ts` → PASS.

- [ ] **Step 5: Commit**

```bash
git add src/core/crypto.ts tests/core/crypto.test.ts
git commit -m "feat(core): cifratura profilo AES-GCM con chiave derivata da firma"
```

---

### Task 10: Prompt builder + adapter 0G Compute

**Files:**
- Create: `src/core/prompt.ts`, `src/services/zerog-compute.ts`
- Test: `tests/core/prompt.test.ts`, `tests/services/zerog-compute.test.ts`

**Interfaces:**
- Consumes: `ContentGraph`, `PersonaProfile`, `assertValidUISpec`.
- Produces: `buildMessages(graph, profile): { system: string; user: string }`; `requestUISpec(graph, profile, broker): Promise<UISpec>` dove `broker` è l'interfaccia minimale `{ chat(messages): Promise<string> }` (wrapper attorno all'SDK 0G, mockabile).

- [ ] **Step 1: Test del prompt**

`tests/core/prompt.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { buildMessages } from '../../src/core/prompt';
const graph = { url:'u', title:'T', blocks:[{id:'block-0',type:'action',text:'Buy'}] } as any;
const profile = { version:1, language:'it', readingLevel:'simple', accessibility:{dyslexiaFriendly:true,highContrast:false,largeText:true,reduceClutter:true}, expertiseDomains:[], tone:'plain' } as any;
describe('buildMessages', () => {
  it('include profilo, block id e istruzione JSON-only', () => {
    const { system, user } = buildMessages(graph, profile);
    expect(system).toMatch(/JSON/i);
    expect(user).toContain('block-0');
    expect(user).toContain('"language":"it"');
  });
  it('vincola i refId agli id presenti nel graph', () => {
    expect(buildMessages(graph, profile).system).toMatch(/refId.*esist|only.*existing/i);
  });
});
```

- [ ] **Step 2: Esegui (fallisce), poi implementa `prompt.ts`**

Run: `npx vitest run tests/core/prompt.test.ts` → FAIL.

`src/core/prompt.ts`:
```ts
import type { ContentGraph, PersonaProfile } from './types';
export function buildMessages(graph: ContentGraph, profile: PersonaProfile) {
  const system = [
    'Sei un motore di generative UI. Ricevi il contenuto di una pagina (ContentGraph) e un profilo utente.',
    'Rigenera l\'interfaccia producendo SOLO un JSON conforme allo schema UISpec (nessun testo extra).',
    'Puoi riscrivere/semplificare/tradurre il testo secondo il profilo.',
    'REGOLA FERREA: ogni refId e sourceRef DEVE essere un id esistente nel ContentGraph (only existing block ids). Non inventare link/azioni.'
  ].join(' ');
  const user = `PROFILE=${JSON.stringify(profile)}\nCONTENT_GRAPH=${JSON.stringify(graph)}`;
  return { system, user };
}
```
Run di nuovo → PASS.

- [ ] **Step 3: Test dell'adapter (broker mockato)**

`tests/services/zerog-compute.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { requestUISpec } from '../../src/services/zerog-compute';
const graph = { url:'', title:'t', blocks:[{id:'block-0',type:'heading',text:'H',level:1}] } as any;
const profile = { version:1, language:'it', readingLevel:'simple', accessibility:{dyslexiaFriendly:false,highContrast:false,largeText:false,reduceClutter:false}, expertiseDomains:[], tone:'neutral' } as any;
const validJson = JSON.stringify({ theme:{fontScale:1,contrast:'normal',font:'system',lineSpacing:1.4,colorMode:'auto',density:'comfortable'}, language:'it', readingLevel:'simple', hidden:[], sections:[{role:'primary',priority:1,content:[{type:'text',text:'ciao'}],sourceRefs:['block-0']}] });

describe('requestUISpec', () => {
  it('parsa e valida la risposta del broker', async () => {
    const spec = await requestUISpec(graph, profile, { chat: async () => validJson });
    expect(spec.sections[0].content[0]).toMatchObject({ type:'text', text:'ciao' });
  });
  it('estrae il JSON anche se avvolto in ```json fences', async () => {
    const spec = await requestUISpec(graph, profile, { chat: async () => '```json\n'+validJson+'\n```' });
    expect(spec.language).toBe('it');
  });
  it('rilancia su JSON non valido', async () => {
    await expect(requestUISpec(graph, profile, { chat: async () => 'non-json' })).rejects.toBeDefined();
  });
});
```

- [ ] **Step 4: Esegui (fallisce), poi implementa `zerog-compute.ts`**

`src/services/zerog-compute.ts`:
```ts
import type { ContentGraph, PersonaProfile, UISpec } from '../core/types';
import { buildMessages } from '../core/prompt';
import { assertValidUISpec } from '../core/uispec-validate';

export interface Broker { chat(messages: { system: string; user: string }): Promise<string>; }

function extractJson(s: string): unknown {
  const fenced = s.match(/```(?:json)?\s*([\s\S]*?)```/);
  const body = fenced ? fenced[1] : s;
  const start = body.indexOf('{'); const end = body.lastIndexOf('}');
  return JSON.parse(body.slice(start, end + 1));
}
export async function requestUISpec(graph: ContentGraph, profile: PersonaProfile, broker: Broker): Promise<UISpec> {
  const raw = await broker.chat(buildMessages(graph, profile));
  return assertValidUISpec(extractJson(raw), graph);
}
```
Run: `npx vitest run tests/services/zerog-compute.test.ts` → PASS (3 test).

- [ ] **Step 5: Implementa il broker reale 0G (thin, non testato in unit)**

Aggiungi in `src/services/zerog-compute.ts` una factory `createZeroGBroker(signer, providerAddress): Broker` che usa `@0gfoundation/0g-compute-ts-sdk` (path *Direct*): inizializza il broker, seleziona un provider dal catalogo (`listService`/`inference`), invia `messages`, ritorna il contenuto testuale. **Confermare i nomi esatti dei metodi SDK nella doc 0G al momento dell'implementazione** e adattare solo dentro questa factory (il resto del codice dipende dall'interfaccia `Broker`).

- [ ] **Step 6: Verifica manuale su testnet 0G**

Con `≥3–4 OG` sul wallet (vedi runbook Task 17), esegui uno script `scripts/try-inference.ts` che chiama `createZeroGBroker(...).chat(buildMessages(graph, profile))` su un content graph d'esempio e stampa la UISpec. Expected: JSON valido conforme allo schema. Se il provider non risponde/insufficiente credito → si userà il fallback fixture (già pronto).

- [ ] **Step 7: Commit**

```bash
git add src/core/prompt.ts src/services/zerog-compute.ts tests/core/prompt.test.ts tests/services/zerog-compute.test.ts scripts/try-inference.ts
git commit -m "feat(services): prompt builder + adapter 0G Compute (parse/valida, broker mockabile)"
```

---

### Task 11: Adapter 0G Storage (upload/download cifrato)

**Files:**
- Create: `src/services/zerog-storage.ts`
- Test: `tests/services/zerog-storage.test.ts`

**Interfaces:**
- Consumes: `encryptJson`/`decryptJson` (core/crypto).
- Produces: `storeProfile(profile, key, backend): Promise<string /*uri*/>` e `loadProfile(uri, key, backend): Promise<PersonaProfile>`, con `backend` = interfaccia `{ put(bytes): Promise<string>; get(uri): Promise<string> }` (wrapper SDK, mockabile).

- [ ] **Step 1: Test con backend in-memory**

`tests/services/zerog-storage.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { storeProfile, loadProfile } from '../../src/services/zerog-storage';
import { deriveKey } from '../../src/core/crypto';
const profile = { version:1, language:'it', readingLevel:'simple', accessibility:{dyslexiaFriendly:true,highContrast:false,largeText:true,reduceClutter:true}, expertiseDomains:[], tone:'plain' } as any;

function memBackend() { const m = new Map<string,string>(); let i=0;
  return { async put(b:string){ const u=`mem://${i++}`; m.set(u,b); return u; }, async get(u:string){ return m.get(u)!; } }; }

describe('0G storage round-trip cifrato', () => {
  it('store poi load ritorna lo stesso profilo', async () => {
    const key = await deriveKey('0x'+'ab'.repeat(65));
    const be = memBackend();
    const uri = await storeProfile(profile, key, be);
    expect(await loadProfile(uri, key, be)).toEqual(profile);
  });
  it('il payload in storage è cifrato (non contiene testo in chiaro)', async () => {
    const key = await deriveKey('0x'+'ab'.repeat(65));
    const be = memBackend(); const uri = await storeProfile(profile, key, be);
    expect(await be.get(uri)).not.toContain('dyslexiaFriendly');
  });
});
```

- [ ] **Step 2: Esegui (fallisce), poi implementa**

`src/services/zerog-storage.ts`:
```ts
import type { PersonaProfile } from '../core/types';
import { encryptJson, decryptJson } from '../core/crypto';
export interface StorageBackend { put(blob: string): Promise<string>; get(uri: string): Promise<string>; }
export async function storeProfile(p: PersonaProfile, key: CryptoKey, be: StorageBackend): Promise<string> {
  return be.put(await encryptJson(key, p));
}
export async function loadProfile(uri: string, key: CryptoKey, be: StorageBackend): Promise<PersonaProfile> {
  return decryptJson<PersonaProfile>(key, await be.get(uri));
}
```
Run: `npx vitest run tests/services/zerog-storage.test.ts` → PASS.

- [ ] **Step 3: Implementa il backend reale 0G Storage**

Aggiungi `createZeroGStorageBackend(signer)` che usa `@0glabs/0g-ts-sdk` per caricare/scaricare il blob (ritorna un URI/root-hash come stringa). **Confermare API SDK nella doc** e isolare qui. Verifica manuale: `scripts/try-storage.ts` fa `storeProfile`→`loadProfile` su testnet e stampa uguaglianza.

- [ ] **Step 4: Commit**

```bash
git add src/services/zerog-storage.ts tests/services/zerog-storage.test.ts scripts/try-storage.ts
git commit -m "feat(services): storage profilo cifrato su 0G (backend mockabile)"
```

---

### Task 12: Bridge content↔injected + wallet MAIN world

**Files:**
- Create: `src/content/bridge.ts`, `src/content/injected.ts`
- Test: `tests/content/bridge.test.ts`

**Interfaces:**
- Produces: `callInjected<T>(method, params): Promise<T>` (lato content, ISOLATED) e handler in `injected.ts` (MAIN) per `eth_requestAccounts`, `personal_sign`, `ens_setText`. Protocollo: `window.postMessage({ source:'ensight', dir:'req'|'res', id, method, params|result|error })`.

- [ ] **Step 1: Test del protocollo bridge (jsdom simula postMessage)**

`tests/content/bridge.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { callInjected } from '../../src/content/bridge';
describe('callInjected', () => {
  it('risolve quando arriva la risposta con lo stesso id', async () => {
    window.addEventListener('message', (e:any) => {
      const m = e.data; if (m?.source==='ensight' && m.dir==='req')
        window.postMessage({ source:'ensight', dir:'res', id:m.id, result:['0xabc'] }, '*');
    });
    await expect(callInjected('eth_requestAccounts', [])).resolves.toEqual(['0xabc']);
  });
  it('rigetta se la risposta contiene error', async () => {
    window.addEventListener('message', (e:any) => {
      const m = e.data; if (m?.source==='ensight' && m.dir==='req')
        window.postMessage({ source:'ensight', dir:'res', id:m.id, error:'rifiutato' }, '*');
    });
    await expect(callInjected('personal_sign', ['x'])).rejects.toThrow('rifiutato');
  });
});
```

- [ ] **Step 2: Esegui (fallisce), poi implementa il bridge**

`src/content/bridge.ts`:
```ts
let seq = 0;
export function callInjected<T = unknown>(method: string, params: unknown[]): Promise<T> {
  const id = `ens-${seq++}`;
  return new Promise<T>((resolve, reject) => {
    const onMsg = (e: MessageEvent) => {
      const m = e.data;
      if (m?.source !== 'ensight' || m.dir !== 'res' || m.id !== id) return;
      window.removeEventListener('message', onMsg);
      m.error ? reject(new Error(m.error)) : resolve(m.result as T);
    };
    window.addEventListener('message', onMsg);
    window.postMessage({ source: 'ensight', dir: 'req', id, method, params }, '*');
  });
}
```
Run: `npx vitest run tests/content/bridge.test.ts` → PASS.

- [ ] **Step 3: Implementa injected.ts (MAIN world)**

`src/content/injected.ts`:
```ts
import { ethers } from 'ethers';
import { CONFIG } from '../config';
window.addEventListener('message', async (e) => {
  const m = (e as MessageEvent).data;
  if (m?.source !== 'ensight' || m.dir !== 'req') return;
  const reply = (p: object) => window.postMessage({ source:'ensight', dir:'res', id:m.id, ...p }, '*');
  try {
    const eth = (window as any).ethereum;
    if (m.method === 'eth_requestAccounts') return reply({ result: await eth.request({ method:'eth_requestAccounts' }) });
    if (m.method === 'personal_sign') {
      const [account] = await eth.request({ method:'eth_requestAccounts' });
      return reply({ result: await eth.request({ method:'personal_sign', params:[m.params[0], account] }) });
    }
    if (m.method === 'ens_setText') {
      const provider = new ethers.BrowserProvider(eth);
      const signer = await provider.getSigner();
      const resolver = await provider.getResolver(CONFIG.ensName); // resolver del nome
      const iface = new ethers.Interface(['function setText(bytes32 node,string key,string value)']);
      const node = ethers.namehash(CONFIG.ensName);
      const [key, value] = m.params;
      const tx = await signer.sendTransaction({ to: resolver!.address, data: iface.encodeFunctionData('setText',[node,key,value]) });
      return reply({ result: (await tx.wait())!.hash });
    }
    reply({ error: `metodo sconosciuto: ${m.method}` });
  } catch (err: any) { reply({ error: err?.message ?? String(err) }); }
});
```
Content script inietta `injected.ts` nel MAIN world:
```ts
const s = document.createElement('script');
s.src = chrome.runtime.getURL('src/content/injected.ts');
document.documentElement.appendChild(s);
```

- [ ] **Step 4: Verifica manuale wallet**

Build, apri pagina mock con MetaMask (rete Sepolia) attivo, da console content: `callInjected('eth_requestAccounts',[])` → apre MetaMask e ritorna l'account; `callInjected('personal_sign',['ENSight profile encryption key v1'])` → firma.

- [ ] **Step 5: Commit**

```bash
git add src/content/bridge.ts src/content/injected.ts tests/content/bridge.test.ts
git commit -m "feat(content): bridge postMessage + injected MAIN world (accounts/sign/setText)"
```

---

### Task 13: Servizio ENS (lettura puntatore + scrittura record)

**Files:**
- Create: `src/services/ens.ts`
- Test: `tests/services/ens.test.ts`

**Interfaces:**
- Consumes: `callInjected` (per la scrittura), `CONFIG.recordKeys`, ethers `JsonRpcProvider` (lettura via RPC pubblico).
- Produces: `readProfilePointer(name, provider): Promise<{ profileUri?: string; human?: string }>`, `writeProfilePointer(name, uri, human): Promise<string /*txHash*/>` (usa `callInjected('ens_setText', ...)`).

- [ ] **Step 1: Test lettura (provider mockato) + chiamate scrittura (bridge mockato)**

`tests/services/ens.test.ts`:
```ts
import { describe, it, expect, vi } from 'vitest';
vi.mock('../../src/content/bridge', () => ({ callInjected: vi.fn(async () => '0xTX') }));
import { readProfilePointer, writeProfilePointer } from '../../src/services/ens';
import { callInjected } from '../../src/content/bridge';

const provider = { getResolver: async () => ({ getText: async (k:string) =>
  k === 'app.ensight.profile' ? 'mem://1' : k === 'app.ensight.human' ? 'nullifier-x' : null }) } as any;

describe('ENS service', () => {
  it('legge i due record dal resolver', async () => {
    expect(await readProfilePointer('n.eth', provider)).toEqual({ profileUri:'mem://1', human:'nullifier-x' });
  });
  it('scrive il record profilo via bridge e ritorna il txHash', async () => {
    const tx = await writeProfilePointer('n.eth', 'mem://2', 'nullifier-y');
    expect(tx).toBe('0xTX');
    expect(callInjected).toHaveBeenCalledWith('ens_setText', ['app.ensight.profile','mem://2']);
  });
});
```

- [ ] **Step 2: Esegui (fallisce), poi implementa**

`src/services/ens.ts`:
```ts
import type { JsonRpcProvider } from 'ethers';
import { CONFIG } from '../config';
import { callInjected } from '../content/bridge';
export async function readProfilePointer(name: string, provider: JsonRpcProvider) {
  const r = await provider.getResolver(name);
  if (!r) return {};
  const [profileUri, human] = await Promise.all([
    r.getText(CONFIG.recordKeys.profile), r.getText(CONFIG.recordKeys.human)]);
  return { profileUri: profileUri ?? undefined, human: human ?? undefined };
}
export async function writeProfilePointer(_name: string, uri: string, human: string): Promise<string> {
  await callInjected('ens_setText', [CONFIG.recordKeys.human, human]);
  return callInjected<string>('ens_setText', [CONFIG.recordKeys.profile, uri]);
}
```
Run: `npx vitest run tests/services/ens.test.ts` → PASS.

- [ ] **Step 3: Verifica manuale su Sepolia**

Con nome ENS su Sepolia e Sepolia ETH: `scripts/try-ens.ts` scrive e rilegge i due record; controlla anche su `sepolia.app.ens.domains`.

- [ ] **Step 4: Commit**

```bash
git add src/services/ens.ts tests/services/ens.test.ts scripts/try-ens.ts
git commit -m "feat(services): lettura/scrittura text record ENS (puntatore profilo + attestation)"
```

---

### Task 14: World ID (attestation da proof)

**Files:**
- Create: `src/services/world-id.ts`
- Test: `tests/services/world-id.test.ts`

**Interfaces:**
- Produces: `attestationFromProof(proof: { nullifier_hash: string }): string` (estrae l'attestation da salvare in ENS). Il widget IDKit vive nella UI di onboarding (Task 16); qui solo la logica pura.

- [ ] **Step 1: Test**

`tests/services/world-id.test.ts`:
```ts
import { describe, it, expect } from 'vitest';
import { attestationFromProof } from '../../src/services/world-id';
describe('attestationFromProof', () => {
  it('usa il nullifier_hash come attestation', () => {
    expect(attestationFromProof({ nullifier_hash: '0xdead' })).toBe('world:0xdead');
  });
  it('lancia se manca il nullifier', () => {
    expect(() => attestationFromProof({} as any)).toThrow();
  });
});
```

- [ ] **Step 2: Esegui (fallisce), poi implementa**

`src/services/world-id.ts`:
```ts
export function attestationFromProof(proof: { nullifier_hash?: string }): string {
  if (!proof?.nullifier_hash) throw new Error('proof World senza nullifier_hash');
  return `world:${proof.nullifier_hash}`;
}
```
Run → PASS.

- [ ] **Step 3: Commit**

```bash
git add src/services/world-id.ts tests/services/world-id.test.ts
git commit -m "feat(services): derivazione attestation da proof World ID"
```

---

### Task 15: Popup (stato + on/off + apri onboarding)

**Files:**
- Create: `src/ui/popup/index.html`, `src/ui/popup/popup.tsx`
- Test: verifica manuale (UI Preact minimale)

**Interfaces:**
- Consumes: `chrome.tabs`, `chrome.runtime`, `chrome.storage`.
- Produces: pulsante toggle (invia `ensight:toggle` al content script della tab attiva), selettore persona (dev), stato "profilo trovato/non trovato", link "Configura profilo" che apre l'onboarding.

- [ ] **Step 1: Implementa popup**

`src/ui/popup/index.html`: root `<div id="app">` + `<script type="module" src="./popup.tsx">`.
`src/ui/popup/popup.tsx` (Preact): bottone "Adatta questa pagina" → `chrome.tabs.query({active:true,currentWindow:true})` poi `chrome.tabs.sendMessage(tab.id, { type:'ensight:toggle' })`; `<select>` persona che scrive `chrome.storage.local`; bottone "Configura profilo" → `chrome.tabs.create({ url: chrome.runtime.getURL('src/ui/onboarding/index.html') })`.

- [ ] **Step 2: Verifica manuale**

Build, apri popup su una tab con la pagina mock: il toggle adatta/ripristina; il selettore persona cambia la resa.

- [ ] **Step 3: Commit**

```bash
git add src/ui/popup && git commit -m "feat(ui): popup con toggle e selettore persona"
```

---

### Task 16: Onboarding (connect → World → form → encrypt → 0G → ENS)

**Files:**
- Create: `src/ui/onboarding/index.html`, `src/ui/onboarding/onboarding.tsx`
- Test: verifica manuale (flusso end-to-end su testnet)

**Interfaces:**
- Consumes: `callInjected` (connect/sign), `IDKitWidget`, `attestationFromProof`, `deriveKey`, `storeProfile` + `createZeroGStorageBackend`, `writeProfilePointer`, `SIGN_MESSAGE`.
- Produces: profilo cifrato caricato su 0G e puntatore+attestation scritti su ENS.

- [ ] **Step 1: Implementa il wizard (5 step)**

`onboarding.tsx` (Preact), stato a step:
1. **Connect**: `callInjected('eth_requestAccounts', [])` → mostra address + ENS name (`CONFIG.ensName`).
2. **Verify human**: `<IDKitWidget app_id={CONFIG.worldAppId} action={CONFIG.worldAction} onSuccess={p => setAttestation(attestationFromProof(p))}>` (staging).
3. **Profilo**: form controllato che produce un `PersonaProfile` (checkbox accessibilità, select lingua/readingLevel/tone, input domini).
4. **Cifra & carica**: `const sig = await callInjected('personal_sign',[SIGN_MESSAGE]); const key = await deriveKey(sig); const uri = await storeProfile(profile, key, createZeroGStorageBackend(signer));`
5. **Scrivi ENS**: `await writeProfilePointer(CONFIG.ensName, uri, attestation)` → mostra txHash + link Sepolia explorer.

- [ ] **Step 2: Aggiorna il content script per il percorso reale**

In `content-script.ts`, `loadProfile` diventa: leggi `readProfilePointer(CONFIG.ensName, new JsonRpcProvider(CONFIG.sepoliaRpc))`; se c'è `profileUri`, firma→`deriveKey`→`loadProfile` da 0G; **cache in `chrome.storage.session`** per non rifirmare a ogni pagina. `getSpec` diventa: prova `requestUISpec(graph, profile, createZeroGBroker(...))`, e **su qualunque errore → `fixtureFor(location.href, persona)`** (fallback demo). Aggiorna il test `tests/content/adapt.test.ts` non serve (adapt resta invariata); aggiungi un test che il content script scelga il fallback quando `getSpec` primario rigetta:

`tests/content/fallback.test.ts`:
```ts
// @vitest-environment jsdom
import { describe, it, expect } from 'vitest';
import { withFallback } from '../../src/content/content-script-helpers';
describe('withFallback', () => {
  it('usa il fallback se il primario rigetta', async () => {
    const r = await withFallback(async () => { throw new Error('x'); }, async () => ({ ok: true }));
    expect(r).toEqual({ ok: true });
  });
  it('usa il primario se ha successo', async () => {
    const r = await withFallback(async () => ({ primary: true }), async () => ({ ok: true }));
    expect(r).toEqual({ primary: true });
  });
});
```
Estrai `withFallback(primary, fallback)` in `src/content/content-script-helpers.ts`:
```ts
export async function withFallback<T>(primary: () => Promise<T>, fallback: () => Promise<T>): Promise<T> {
  try { return await primary(); } catch { return await fallback(); }
}
```
Run: `npx vitest run tests/content/fallback.test.ts` → PASS.

- [ ] **Step 3: Verifica manuale end-to-end su testnet**

Segui il runbook (Task 17): connect → World verify → compila profilo → carica su 0G → scrivi su ENS. Poi apri la pagina mock: il content script legge il profilo da ENS, decifra da 0G, chiama 0G Compute; se l'inferenza risponde usa la UISpec live, altrimenti fixture. **In entrambi i casi la pagina si adatta.**

- [ ] **Step 4: Commit**

```bash
git add src/ui/onboarding src/content/content-script.ts src/content/content-script-helpers.ts tests/content/fallback.test.ts
git commit -m "feat: onboarding completo + percorso profilo reale con fallback fixture"
```

---

### Task 17: Config finale, runbook demo, script di preparazione

**Files:**
- Create: `docs/RUNBOOK.md`, `scripts/dump-graph.ts`, `scripts/try-inference.ts`, `scripts/try-storage.ts`, `scripts/try-ens.ts` (se non già creati)
- Modify: `.env.example`

**Interfaces:**
- Produces: procedura riproducibile per allestire la demo a costo zero.

- [ ] **Step 1: Scrivi il runbook**

`docs/RUNBOOK.md` con: (a) **Giorno 1 — token**: registra nome ENS su Sepolia (`sepolia.app.ens.domains`), prendi Sepolia ETH (faucet no-KYC), crea app World staging nel Developer Portal (copia `app_id`/`action`), prendi ≥3–4 OG testnet dal **faucet 0G (richiede X)** e **richiedi top-up al booth/Discord 0G**; (b) compila `.env`; (c) `npm run build` + load unpacked; (d) esegui onboarding; (e) apri le pagine mock e mostra le due personas; (f) checklist fallback (fixture pronte).

- [ ] **Step 2: Verifica: intera suite verde + build**

Run: `npm test && npm run build`
Expected: tutti i test PASS, build ok.

- [ ] **Step 3: Commit**

```bash
git add docs/RUNBOOK.md scripts .env.example
git commit -m "docs: runbook demo a costo zero + script di preparazione"
```

---

### Task 18: README e script della demo

**Files:**
- Create/Modify: `README.md`, `docs/DEMO-SCRIPT.md`

- [ ] **Step 1: README** — cos'è ENSight, architettura (link alla spec), ruoli sponsor (ENS/0G/World), come si installa/costruisce, budget zero.
- [ ] **Step 2: DEMO-SCRIPT.md** — copione ~3 min: pagina raw → Persona A → Persona B (stessa pagina, UI diverse) → "sotto il cofano" (record ENS, blob 0G, TEE) → accenno Fase 2 (loop comportamentale).
- [ ] **Step 3: Commit**

```bash
git add README.md docs/DEMO-SCRIPT.md
git commit -m "docs: README e script demo ENSight"
```

---

## Self-Review (eseguita)

**1. Copertura spec:** identità/preferenze ENS → Task 13,16; profilo generale (accessibilità+preferenze) → Task 2,5,7,16; re-layout generativo completo → Task 4,6,8,10; 0G Storage cifrato → Task 9,11; 0G Compute TEE → Task 10; World umano → Task 14,16; text record puntatore → Task 13; refId reali/no allucinazioni → Task 4,6; privacy/chiave da firma → Task 9,12,16; demo mock 2 pagine + 2 personas → Task 7,8,15; fallback pre-calcolato → Task 7,8,16; budget zero/testnet → Task 17. Fase 2 (loop comportamentale) è esplicitamente fuori scope: nessun task, corretto.

**2. Placeholder scan:** i punti "confermare API SDK nella doc" (Task 10.5, 11.3) sono isolati dietro interfacce `Broker`/`StorageBackend` già testate con mock; i valori `.env` sono config d'ambiente da dashboard, non placeholder logici. Nessun "TODO/implementa dopo" nei passi di codice.

**3. Coerenza tipi:** `ContentGraph`/`PersonaProfile`/`UISpec` (Task 2) usati identici ovunque; `data-ensight-id` scritto dall'extractor (Task 3) e letto dal renderer (Task 6); `Broker`/`StorageBackend`/`SpecProvider` con firme coerenti tra definizione e uso; `callInjected(method,params)` uniforme in bridge/ens/injected; `withFallback` usato nel content script.

## Execution Handoff

Piano completo e salvato in `docs/superpowers/plans/2026-07-24-ensight.md`.
